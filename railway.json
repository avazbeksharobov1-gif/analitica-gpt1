{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema=prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=prisma/schema.prisma && node index.js"
  }
}
